# 待办清单

## 后期待做

- [ ] 操作日志
  - 说明：记录用户在 Worker / CLI / 回调里触发的关键业务动作，作为可审计历史，而不是普通控制台日志。
  - 方向：先写 `requirements`，再写 `design`，最后拆分 `tasks`。
  - 关注点：记录内容、脱敏规则、存储位置、查询入口、保留策略。
- [ ] 多用户
  - 说明：支持多个独立用户或管理员账号，避免所有数据都绑定单一账号。
  - 方向：先明确用户隔离模型和登录/权限边界，再拆需求和设计。
  - 关注点：用户标识、权限范围、数据隔离、默认账号迁移、兼容现有单用户行为。
