# AGENTS.md

Mandatory project rules:

- Follow [`python/src/RULES.md`](python/src/RULES.md) for anything under `python/src`.
- Treat `python/src` as the only Python source root.
- Keep the repository root clean.
- Keep Python generated artifacts in `python/artifacts/`.
- Keep Worker JS code under `worker-dashboard/` only.

If a change conflicts with these rules, update the rules first, then the code.
